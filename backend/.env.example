# Gmail address that will send the password reset emails
EMAIL_USER=your_gmail_address@gmail.com

# Gmail App Password (see instructions below)
EMAIL_PASS=your_gmail_app_password

# The base URL of your frontend (for password reset link)
FRONTEND_URL=http://localhost:3000

# JWT secret for your backend
JWT_SECRET=your-secret-key

# ---
# Gmail App Password Setup:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification if not already enabled.
# 3. Under "Signing in to Google", select "App Passwords".
# 4. Generate a new App Password for "Mail" and "Other" (give it a name like "ATB App").
# 5. Paste the generated password (16 characters) as EMAIL_PASS above.
#
# NOTE: Do NOT use your regular Gmail password. Always use an App Password for security.
